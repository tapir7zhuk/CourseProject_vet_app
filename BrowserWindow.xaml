<Window x:Class="CW_hammer.BrowserWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CW_hammer"
        mc:Ignorable="d"
        Title="VetBrowser" Height="700" Width="1100"
        Background="{DynamicResource BackgroundBrush}">
    
     <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- ТАБ-БАР -->
        <Border Grid.Row="0" Background="{DynamicResource PrimaryBrush}">
            <DockPanel Height="44" LastChildFill="True">

                <Button x:Name="BackBtn"
                        Content="←" Width="40" Margin="4,6"
                        DockPanel.Dock="Left"
                        Click="BackBtn_Click"
                        Style="{DynamicResource AppButton}"/>

                <Button x:Name="ForwardBtn"
                        Content="→" Width="40" Margin="4,6"
                        DockPanel.Dock="Left"
                        Click="ForwardBtn_Click"
                        Style="{DynamicResource AppButton}"/>

                <ItemsControl x:Name="TabList"
                              ItemsSource="{Binding Tabs}"
                              VerticalAlignment="Stretch">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel Orientation="Horizontal"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Margin="2,4,2,0" Padding="14,0"
                                    CornerRadius="4,4,0,0" Cursor="Hand"
                                    MouseDown="Tab_Click"
                                    Tag="{Binding}">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <!-- Неактивний таб -->
                                        <Setter Property="Background"
                                                Value="{DynamicResource BackgroundBrush}"/>
                                        <Setter Property="Opacity" Value="0.6"/>
                                        <!-- Активний таб -->
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsActive}"
                                                         Value="True">
                                                <Setter Property="Background"
                                                        Value="{DynamicResource SurfaceBrush}"/>
                                                <Setter Property="Opacity" Value="1"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding Title}"
               VerticalAlignment="Center"
               Foreground="{DynamicResource TextBrush}"/>
                                    <Button Content="✕"
            Width="18" Height="18"
            Margin="6,0,0,0"
            FontSize="10"
            Background="Transparent"
            BorderThickness="0"
            Cursor="Hand"
            Foreground="{DynamicResource TextBrush}"
            Click="CloseTab_Click"
            Tag="{Binding}"/>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

            </DockPanel>
        </Border>

        <!-- ФРЕЙМ -->
        <Frame x:Name="MainFrame"
               Grid.Row="1"
               NavigationUIVisibility="Hidden"
               Navigated="MainFrame_Navigated"/>

    </Grid>
</Window>